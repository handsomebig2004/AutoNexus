"""External interface entry points.

Interfaces translate outside-world inputs such as CLI, stdin, files, or future
web requests into internal schemas. They should stay thin and avoid embedding
agent logic.
"""
